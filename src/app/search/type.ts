export type GroupDetail = {
  name: string;
  participant: number;
};

export type UserDetail = {
  userId: string;
  userName: string;
};

export type userData = {
  id: string;
  avatar: string;
  nickname: string;
};
